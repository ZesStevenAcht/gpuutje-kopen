[project]
name = "marktplaats-scraper"
version = "0.1.0"
description = "GPU price tracker from Marktplaats"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marktplaats>=0.4.0",
    "flask>=3.0.0",
    "plotly>=5.17.0",
    "pandas>=2.0.0",
    "rapidfuzz>=2.0.0",
]
